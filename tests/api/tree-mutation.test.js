
describe("The Node Mutator", () => {

  test("The Node Mutator only works if passed a valid JSON object.", () => {

  });

  test("The node mutator creates a node from the JSON.", () => {

  });

  test("If the JSON is invalid, the node mutator errors out.", () => {

  });

  test("If the mutator is successful, it returns both a node and the required global context variables it needs to run.", () => {

  });
});